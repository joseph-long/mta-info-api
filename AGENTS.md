`curl` is blocked in this environment but network access is not. Use Python instead to download files.

For browser testing, use Playwright to write end-to-end tests and validate that they work in Firefox.
